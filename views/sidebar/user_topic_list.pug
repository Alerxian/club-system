
.panel
  .panel-hd <span class="ico-list"></span> 作者其它主题
  .panel-bd.user-info
    ul
      each item in topic.author_topic_list
        - title = item.title.replace(/\s+/g,'')
        li: a(href="/topic/"+ item._id)= title.length >=20 ? title.slice(0,20)+'...' : title